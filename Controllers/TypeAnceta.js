const ralataion = require('./ralationType.json');

const typeAnceta = (id) =>{
    if(ralataion.type1.includes(id)){
        
    }
    else if(ralataion.type2.includes(id)){

    }
    else{
        return false;
    }
}
module.exports = typeAnceta;